# Copy to .env and fill. .env is gitignored.
HF_TOKEN=
WANDB_API_KEY=
# Teacher-judge for the commonsense/causal head (only paid label stream). Leave blank to use the NLI proxy.
JUDGE_API_BASE=https://api.deepseek.com
JUDGE_API_KEY=
JUDGE_MODEL=deepseek-chat
